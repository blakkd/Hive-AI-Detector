getDeleteIcon(UploadedImagePreviewOuter, handleDeleteFile);
getDeleteIcon(
  UploadedAudioPreviewOuter,
  handleDeleteFile,
  "hvaid-delete-icon-horizontal"
);
getDeleteIcon(UploadedVideoPreviewOuter, handleDeleteFile);

getDeleteIcon(IconContainer, handleDeleteFile, "hvaid-delete-icon-horizontal");

/* image icon */
getImageIcon(ImageIcon);

/* audio icon */
getAudioIcon(AudioIcon);

/* video icon */
getVideoIcon(VideoIcon);
